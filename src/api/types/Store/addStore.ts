export interface RequestStoreAddData {
  email: string;
  zoneID: string;
  storeName: string;
  description: string;
}
export interface ResponseStoreAddData {
  status: string;
  message?: string;
}
